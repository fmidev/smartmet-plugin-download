port            = 8088;

plugins:
{
  download:
  {
    configfile   = "download.conf";
    libfile      = "../../../download.so";
  }; 
};

engines:
{
  grid:
  {
    configfile  = "grid/engine/grid-engine.conf";
  };

  geonames:
  {
    configfile  = "../../cnf/geonames.conf";
  };

  querydata:
  {
    configfile   = "../../cnf/querydata.conf";
  };
};
