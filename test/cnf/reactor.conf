// Options

logrequests	= false;
maxthreads	= 20;

debug	= false;
verbose		= false;
// quiet	= false;
// timeout	= 60;
// servertype	= "hahs";

// Test config

libdir = "/usr/share/smartmet";

plugins:
{
	download:
	{
		disabled	= false;
		libfile		= "../../download.so";
		configfile	= "download.conf";
	};
};

engines:
{
        grid:
        {
                disabled        = false;
                configfile      = "grid-engine.conf";
        };

	geonames:
	{
	        disabled        = false;
	        configfile      = "geonames.conf";
	};
	
	querydata:
	{
		disabled	= false;
		configfile	= "querydata.conf";
	};
};
