// Options

logrequests	= false;
maxthreads	= 20;

debug	= true;
verbose		= true;
// quiet	= false;
// timeout	= 60;
// servertype	= "hahs";

// Test config

libdir = "/usr/share/smartmet";

plugins:
{
	download:
	{
		disabled	= false;
		libfile		= "../download.so";
		configfile	= "cnf/download.conf";
	};
};

engines:
{
	geonames:
	{
	        disabled        = false;
	        configfile      = "cnf/geonames.conf";
	};
	
	querydata:
	{
		disabled	= false;
		configfile	= "../../../engines/querydata/test/querydata.conf";
	};
};
