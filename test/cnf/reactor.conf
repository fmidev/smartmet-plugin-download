// Options

logrequests	= false;
maxthreads	= 20;

debug	= true;
verbose		= true;
// quiet	= false;
// timeout	= 60;
// servertype	= "hahs";

// Test config

libdir = "/usr/share/smartmet";

plugins:
{
	download:
	{
		disabled	= false;
		libfile		= "../../download.so";
		configfile	= "download.conf";
	};
};

engines:
{
	geonames:
	{
	        disabled        = false;
	        configfile      = "geonames.conf";
	};
	
	querydata:
	{
		disabled	= false;
		configfile	= "querydata.conf";
	};
};
